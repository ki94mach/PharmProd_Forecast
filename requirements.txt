# Pip extras for the forecast conda env.
# Create the env first, then install these with that env's pip:
#
#   conda env create -f environment.yml
#   conda activate forecast
#   python -m pip install -r requirements.txt
#   python -m pip install -e .
#
# Do not use system Python (e.g. Fedora 3.14) — this project needs Python 3.10.

# Residual correction MVP (notebooks/residual_prediction.ipynb)
xgboost>=2.0,<3

# M1 Optuna hyperparameter tuning (research only — not under benchmark/)
optuna>=3.6,<5

# Benchmark freeze panels (pkg.benchmark)
pyarrow>=14,<20

# Deep learning (LSTM in forecast.py)
tensorflow>=2.15,<3

# Google Sheets (optional; pkg/google_sheet.py)
gspread>=5.12,<7
gspread-formatting>=1.1,<2
